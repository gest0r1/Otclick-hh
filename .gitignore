# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Env vars / installer state
.env
.env.local
backups/
# Per-install candidate data. install.sh seeds this from the curated defaults;
# users may edit it without making future git updates dirty.
backend/data/candidate-local/

# Sub Repos
hh-applicant-tool/
extension/
# Anchored: an unanchored `app/` also matched backend/app, so new backend files
# were silently ignored. This entry is the OtclickUS copy at the repo root.
/app/

.wordstat.env
.gstack/

# recon dumps (contain a logged-in hh page)
backend/recon_oauth_out/
backend/recon_web_out/
